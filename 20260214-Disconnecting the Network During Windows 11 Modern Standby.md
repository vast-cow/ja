---
pubDatetime: 2026-02-14T17:33:19+09:00
title: "Windows 11 Modern Standby中にネットワークを切断する設定"
description: "Windows 11 の Modern Standby（S0 低電力アイドル）は、スリープ中でも通知や同期のためにネットワークが維持されることがあります。 「スリープ中はネットワークを切ってほしい」「夜間のバックグラウンド通信を止めたい」といった用途では、Connected Standby を無効化…"
---

Windows 11 の **Modern Standby（S0 低電力アイドル）**は、スリープ中でも通知や同期のためにネットワークが維持されることがあります。
「スリープ中はネットワークを切ってほしい」「夜間のバックグラウンド通信を止めたい」といった用途では、**Connected Standby を無効化して Disconnected に寄せる**設定が有効です。

この記事では、Windows 11 で **Modern Standby 中のネットワーク接続を切断する方法**を解説します。

## この記事でやること

* Modern Standby（S0）中の **ネットワーク接続を許可しない**
* 電源接続時／バッテリ使用時の両方を制御する
* 設定後に Modern Standby であることを確認する

## グループポリシーで「スタンバイ中のネットワーク接続」を無効化（Pro/Enterprise向け）

最も確実なのは **グループポリシー**での設定です（Windows 11 Pro / Enterprise で利用可能）。

### 手順

1. `Win + R` を押して **ファイル名を指定して実行**を開く
2. `gpedit.msc` を入力して **ローカル グループ ポリシー エディター**を起動
3. 次の場所へ移動
   **コンピューターの構成** → **管理用テンプレート** → **システム** → **電源の管理** → **スリープの設定**
4. 以下のポリシーを探して、どちらも **「無効」** に設定する

* **コネクト スタンバイ中にネットワーク接続を許可する (電源接続時)**
  （英語表記：Allow network connectivity during connected-standby (plugged in)）

* **コネクト スタンバイ中にネットワーク接続を許可する (バッテリ使用時)**
  （英語表記：Allow network connectivity during connected-standby (on battery)）

5. 反映させる
   管理者でコマンドプロンプトを開き、以下を実行（または再起動）

```bat
gpupdate /force
```

### 期待される効果

この設定により、Modern Standby 中の挙動が **Connected（接続維持）→ Disconnected（切断）**に寄り、スリープ中の通信やバックグラウンド同期が抑制されます。

## Modern Standby（S0）かどうかを確認する

設定の前提として、あなたのPCが Modern Standby を採用しているか確認できます。

管理者のコマンドプロンプトで実行：

```bat
powercfg /a
```

出力に **「スタンバイ (S0 低電力アイドル)」**が含まれていれば Modern Standby 対応です。

## 注意点（副作用）

* スリープ中の **通知・同期・メール受信・更新**などが止まる可能性があります（意図どおり）
* NICやドライバ実装によっては、完全に切断にならず挙動が残るケースもあります

## まとめ

Windows 11 の Modern Standby 中にネットワークを切断したい場合、基本は **グループポリシーで以下2項目を「無効」**にするのが最短です。

* **コネクト スタンバイ中にネットワーク接続を許可する (電源接続時)**
* **コネクト スタンバイ中にネットワーク接続を許可する (バッテリ使用時)**

スリープ中の不要な通信を抑えたい人は、一度試してみる価値があります。
