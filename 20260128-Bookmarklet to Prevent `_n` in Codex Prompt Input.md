---
pubDatetime: 2026-01-28T02:13:55+09:00
title: "Codex のプロンプト入力で `\\n` を防ぐためのブックマークレット"
description: "目的、仕組み、ブックマークレット版を中心に、記事全体の背景・手順・確認方法・注意点を整理します。"
---

## 概要

この記事では、Codex のプロンプト入力欄に入力されたテキストから不要な `\n` を削除する、シンプルな JavaScript ブックマークレットを紹介します。
目的は、プロンプトを 1 行の連続したテキストとして送信し、意図しない改行によるフォーマット上の問題を防ぐことです。

## 目的

Codex にプロンプトを入力する際、意図せず `\n` が挿入されることがあります。
これらの改行は、プロンプトの解釈に影響を与える可能性があります。

このブックマークレットは、Codex にリクエストを送信する直前に、入力テキストをクリーンアップするために設計されています。

想定されるワークフローは以下のとおりです。

1. 通常どおりプロンプトを入力する
2. ブックマークレットを実行する
3. クリーンアップされたプロンプトを Codex に送信する

## 仕組み

このスクリプトはプロンプト入力エリアを走査し、その中に含まれる各段落要素を処理します。
各要素について、HTML コンテンツからキャリッジリターン（`\r`）および改行文字（`\n`）を削除します。

### JavaScript コード

```javascript
Array.from(document.querySelectorAll("#prompt-textarea > p")).forEach((e) => {
  console.log(JSON.stringify(e.innerHTML));
  e.innerHTML = e.innerHTML.replaceAll("\r", "").replaceAll("\n", "");
});
```

このコードは次の処理を行います。

* プロンプト入力エリア内のすべての `<p>` 要素を選択
* 元の内容を参照用としてコンソールに出力
* 改行関連の文字をすべて空文字列に置換

## ブックマークレット版

この機能をブラウザで簡単に使えるように、同じロジックをブックマークレットとして保存できます。
新しいブックマークを追加し、URL 欄に以下のコードを貼り付けてください。

```javascript
javascript:Array.from(document.querySelectorAll("%23prompt-textarea%20%3E%20p")).forEach(e=>{console.log(JSON.stringify(e.innerHTML));e.innerHTML=e.innerHTML.replaceAll("%5Cr","").replaceAll("%5Cn","")});
```

保存後、このブックマークレットをクリックすると、Codex に現在入力されているプロンプトテキストが即座にクリーンアップされます。

## まとめ

このブックマークレットは、Codex のプロンプトから意図しない改行文字を素早く確実に削除する方法を提供します。
送信直前に実行することで、入力をよりクリーンに保ち、プロンプトの挙動をより予測しやすくすることができます。

## 補足

代替手段として [ChatGPT 入力フォームオーバーレイのためのブックマークレット](https://qiita.com/vast-cow/items/4e5455033156d11523e9) も用意してあります。
