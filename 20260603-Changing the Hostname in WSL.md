---
pubDatetime: 2026-06-03T15:01:19+09:00
title: "WSLでホストネームを変更する方法"
description: "WSLでは、Linux環境内で使われるホストネームを変更できます。 ホストネームを分かりやすい名前にしておくと、複数のWSLディストリビューションを使い分けるときや、ターミナル上で環境を識別するときに便利です。 設定ファイルを編集する WSLのホストネームは、/etc/wsl.conf に設定します…"
---

WSLでは、Linux環境内で使われるホストネームを変更できます。
ホストネームを分かりやすい名前にしておくと、複数のWSLディストリビューションを使い分けるときや、ターミナル上で環境を識別するときに便利です。

## 設定ファイルを編集する

WSLのホストネームは、`/etc/wsl.conf` に設定します。

次のように `[network]` セクションを追加し、`hostname` に使いたい名前を指定します。

```ini
[network]
hostname = myhostname
```

ここでは例として `myhostname` を指定しています。
実際には、自分が使いたいホストネームに置き換えてください。

## WSLを再起動する

設定を反映するには、WSLを再起動します。

特定のディストリビューションだけを終了する場合は、Windows側のターミナルで次のコマンドを実行します。

```powershell
wsl -t mydistribution
```

`mydistribution` は、対象のWSLディストリビューション名に置き換えてください。

すべてのWSL環境を終了する場合は、次のコマンドを使います。

```powershell
wsl --shutdown
```

その後、WSLを起動し直すと、新しいホストネームが反映されます。

## 注意点

WSLでは、通常のLinuxと同じ方法でホストネームを変更しても、設定が元に戻ってしまうことがあります。

たとえば、次のような方法では永続的な変更にならない場合があります。

```bash
hostname
hostnamectl
/etc/hostname
/etc/hosts
```

WSLでホストネームを固定したい場合は、`/etc/wsl.conf` に設定する方法を使うのが確実です。
