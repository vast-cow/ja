---
pubDatetime: 2026-03-29T16:52:50+09:00
title: "低電力モードが自動解除されても、すぐ再度オンにする"
description: "有効・無効を切り替えられる iPhoneショートカット自動化の作り方 iPhoneの低電力モードは便利ですが、状況によっては自動で解除されることがあります。 そのたびに手動で戻すのは面倒です。 そこで今回は、 低電力モードがオフになったら自動で再度オンにするオートメーション その機能を有効・無効で切…"
---

## 有効・無効を切り替えられる iPhoneショートカット自動化の作り方

iPhoneの**低電力モード**は便利ですが、状況によっては自動で解除されることがあります。
そのたびに手動で戻すのは面倒です。

そこで今回は、

* **低電力モードがオフになったら自動で再度オンにするオートメーション**
* **その機能を有効・無効で切り替えるショートカット**

を組み合わせる方法を、ブログ記事として分かりやすくまとめます。

この構成では、`このiPhone内` に置いた隠しファイルをフラグとして使い、
**自動再有効化を動かすかどうか**を判定します。
切り替え用ショートカットは `.lowpowermode-enabled` と `.lowpowermode-disabled` を切り替え、同時に低電力モード自体のオン・オフも実行する作りです。
また、自動再有効化用ショートカットは `.lowpowermode-enabled` を確認し、条件を満たしたときだけ低電力モードをオンに戻し、通知を表示します。

---

## この記事でできること

この記事の手順どおりに作ると、次の2つが完成します。

### 1. 切り替え用ショートカット

このショートカットをタップすると、

* 自動再有効化を**有効にする**
* 自動再有効化を**無効にする**

の切り替えができます。

内部的には、`このiPhone内` のフラグファイル名を変更して状態を管理します。
有効時は `.lowpowermode-enabled`、無効時は `.lowpowermode-disabled` です。

### 2. 自動再有効化ショートカット

こちらは、低電力モードがオフになったときにオートメーションから呼び出されます。
ただし、**有効フラグが立っているときだけ**低電力モードをオンに戻します。

### 3. 個人用オートメーション

最後に、低電力モードがオフになった瞬間に、自動再有効化ショートカットが走るよう設定します。

---

## この仕組みの全体像

動作の流れはシンプルです。

1. 切り替え用ショートカットで有効・無効を変更する
2. 状態はフラグファイル名で保存する
3. 低電力モードがオフになる
4. オートメーションが起動する
5. 自動再有効化ショートカットがフラグを確認する
6. 有効なら低電力モードをオンに戻す

ポイントは、**オートメーション自体は常に置いておき、実際に動くかどうかだけをフラグで制御する**ことです。
これなら、毎回オートメーションを編集する必要がありません。

---

## 事前準備

まずはフラグ用のファイルを用意します。

### 保存場所

保存場所は **「このiPhone内」** を使います。
アップロードされたショートカット定義でも、ローカルストレージを参照する構成になっています。

### 作成するファイル

次のどちらか一方を作っておけば始められます。

* `.lowpowermode-enabled`
* `.lowpowermode-disabled`

最初から自動再有効化を使いたいなら、`.lowpowermode-enabled` を置いておくのが分かりやすいです。

### 注意

ファイル名は完全一致が必要です。
スペルが違うと、判定も切り替えも動きません。

---

## 1本目：有効・無効を切り替えるショートカットの作り方

まずは、再自動化機能のオン・オフを切り替えるショートカットを作ります。

### このショートカットの役割

このショートカットは、現在の状態を見て次のように動きます。

* `.lowpowermode-enabled` が取得できたら
  → `.lowpowermode-disabled` に名前変更
  → 低電力モードをオフ
* 取得できなければ
  → `.lowpowermode-disabled` を取得
  → `.lowpowermode-enabled` に名前変更
  → 低電力モードをオン

つまり、**フラグ切り替え**と**低電力モードの切り替え**を同時にやるショートカットです。

### 作成手順

#### 1. 新しいショートカットを作る

ショートカットアプリを開き、新規ショートカットを作成します。
名前はたとえば次のようなものが分かりやすいです。

* `低電力モード自動維持 切替`
* `低電力自動再オン 切替`

#### 2. 「フォルダからファイルを取得」を追加

最初のアクションに **「フォルダからファイルを取得」** を追加します。

設定内容:

* 保存場所: `このiPhone内`
* ファイルパス: `.lowpowermode-enabled`
* 見つからない場合にエラー: オフ

#### 3. 「もし」を追加

次に **「もし」** を追加し、条件を次のようにします。

* **ファイルが任意の値なら**

この条件は、`.lowpowermode-enabled` が取得できたかどうかを見るためのものです。

#### 4. 条件を満たす場合の処理

`もし ファイルが任意の値なら` の中には、次の2つを入れます。

1. **「ファイルの名前を変更」**
   `.lowpowermode-enabled` → `.lowpowermode-disabled`
2. **「低電力モードを設定」**
   オフ

これは「現在は有効だから、無効に切り替える」処理です。

#### 5. 「それ以外」の処理

`それ以外` には、次の流れを入れます。

1. **「フォルダからファイルを取得」**
   `.lowpowermode-disabled`
2. **「ファイルの名前を変更」**
   `.lowpowermode-disabled` → `.lowpowermode-enabled`
3. **「低電力モードを設定」**
   オン

これは「現在は無効だから、有効に切り替える」処理です。

### このショートカットの完成形

完成すると、ボタン1つで次の動作が切り替わります。

* 有効 → 無効
* 無効 → 有効

しかも、フラグだけでなく、**低電力モード本体の状態もその場で合わせて切り替わる**ので、使い勝手が良い構成です。

---

## 2本目：低電力モードを自動で再有効化するショートカットの作り方

次に、低電力モードがオフになったときに、自動でオンへ戻すショートカットを作ります。

### このショートカットの役割

このショートカットは、フラグが有効状態かどうかを確認し、
有効なら低電力モードをオンに戻します。

処理の流れは次のとおりです。

* `.lowpowermode-enabled` を取得する
* `もし ファイルが任意の値なら` で判定する
* 条件を満たすなら低電力モードをオンにする
* 通知を表示する

通知タイトルは **「低電力モードをオンに戻しました」** です。

### 作成手順

#### 1. 新しいショートカットを作る

名前はたとえば次のようにします。

* `低電力モードを再有効化`
* `低電力モード自動復帰`

#### 2. 「フォルダからファイルを取得」を追加

最初のアクションに **「フォルダからファイルを取得」** を追加します。

設定内容:

* 保存場所: `このiPhone内`
* ファイルパス: `.lowpowermode-enabled`

#### 3. 「もし」を追加

条件は次のとおりです。

* **ファイルが任意の値なら**

つまり、`.lowpowermode-enabled` が取得できたときだけ、後続の処理を実行します。

#### 4. 条件を満たす場合の処理

`もし` の中に以下を入れます。

1. **「低電力モードを設定」**
   オン
2. **「通知を表示」**
   タイトル: `低電力モードをオンに戻しました`

これで、有効時だけ自動復帰するショートカットになります。

---

## 個人用オートメーションの設定方法

ここまででショートカットは2本できました。
最後に、自動再有効化ショートカットをオートメーションから起動するよう設定します。

### 手順

#### 1. ショートカットアプリで「オートメーション」を開く

画面下部の **「オートメーション」** タブを開きます。

#### 2. 個人用オートメーションを新規作成

**「個人用オートメーションを作成」** を選びます。

#### 3. トリガーを「低電力モード」にする

候補の中から **「低電力モード」** を選びます。

#### 4. 条件を「オフになったとき」にする

ここは重要です。
**低電力モードがオフになったとき** に動くよう設定します。

#### 5. 実行内容に「ショートカットを実行」を追加

アクションとして **「ショートカットを実行」** を追加し、
先ほど作った **「低電力モードを再有効化」** を指定します。

#### 6. 実行確認をオフにする

必要に応じて、

* **実行の前に尋ねる** をオフ
* **実行時に通知** を好みに応じて調整

しておきます。

これで、低電力モードがオフになるたびに自動再有効化ショートカットが呼ばれるようになります。

---

## 実際の使い方

運用方法は簡単です。

### 自動再有効化を使いたいとき

切り替え用ショートカットを実行します。

* `.lowpowermode-disabled` → `.lowpowermode-enabled`
* 低電力モードもオン

という状態になります。

### 自動再有効化を止めたいとき

もう一度、同じ切り替え用ショートカットを実行します。

* `.lowpowermode-enabled` → `.lowpowermode-disabled`
* 低電力モードもオフ

になります。

つまり、**1本の切り替え用ショートカットだけで運用モードを変更できる**わけです。

---

## この構成のメリット

### オートメーションを触らずにオン・オフできる

通常、オートメーションを一時停止したいときは編集が必要です。
この方法なら、切り替え用ショートカットを押すだけで済みます。

### 状態管理が分かりやすい

有効か無効かが、フラグファイル名で明確です。

* `.lowpowermode-enabled` なら有効
* `.lowpowermode-disabled` なら無効

というルールで判断できます。

### 構成を拡張しやすい

この方式は応用も容易です。
たとえば次のような拡張が考えられます。

* 通知内容を変える
* 特定の時間帯だけ再有効化する
* ログをファイルに記録する
* 特定の集中モード中だけ無効にする

---

## 注意点

### フラグファイルの場所は揃える

切り替え用ショートカットと自動再有効化用ショートカットで、
**同じ保存場所を参照していること**が前提です。
片方だけ別フォルダになると動作しません。

### ファイル名は正確に入力する

使う文字列は次の2つです。

* `.lowpowermode-enabled`
* `.lowpowermode-disabled`

大文字・小文字や記号の違いがあると認識されません。

### 通知が頻繁に出る可能性がある

自動再有効化ショートカットには通知アクションが含まれています。
低電力モードが頻繁に解除される環境では、通知がやや目立つかもしれません。

---

## まとめ

この方法は、iPhoneのショートカットだけで

* **低電力モードがオフになったら自動で再オン**
* **その機能を手元のショートカットで有効・無効切り替え**

を実現する構成です。

技術的なポイントは、
**「フォルダからファイルを取得」** でフラグファイルを読み込み、
**「もし ファイルが任意の値なら」** で有効状態を判定している点です。
切り替え用ショートカットはフラグの名前変更と低電力モード切り替えを担当し、再有効化用ショートカットは有効フラグがあるときだけ低電力モードをオンへ戻します。

単に「自動でオンにする」だけでなく、
**必要なときだけ使える運用性**まで含めて作れるのが、この仕組みの利点です。
